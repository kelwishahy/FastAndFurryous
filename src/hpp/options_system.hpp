#pragma once
#include "hpp/tiny_ecs.hpp"

//startscreen16
//void decreaseTimer(Entity entity, int amount);
int decreaseTimer(Entity entity, int amount);

//startscreen16
//void increaseTimer(Entity entity, int amount);
int increaseTimer(Entity entity, int amount);
//startscreem17
int decreasePlayers(Entity entity, int amount);
//void decreasePlayers(Entity entity, int amount);

//void increasePlayers(Entity entity, int amount);
int increasePlayers(Entity entity, int amount);